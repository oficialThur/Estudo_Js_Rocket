const input = document.querySelector("input")

// evento de keydown = evento desparado quando uma tecla e desparada

/*input.addEventListener("keydown", (event) => {

})*/


// evento de keypress = evento desparado quando uma tecla do tipo caractere e precionada (letra, ponto, numero, etc)

input.addEventListener("keypress", () => {

})

input.onchange = (event) => {

}